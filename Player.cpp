#include "Player.h"
// Player and monster class
program selectDegree(string deg) //Used exactly once
{
	if (deg == "cs")
		return cs;
	else if (deg == "is")
		return is;
	else if(deg == "eet")
		return eet;
	return cs;
}








//void gradeCheck(player character);
//void combat(player character, player monster);